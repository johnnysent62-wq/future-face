import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Color (Blue from your design)
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryDark = Color(0xFF1E293B);
  static const Color primaryLight = Color(0xFF60A5FA);

  // Logo colors
  static const Color logoBlue = Color(0xFFBAE6FD);
  static const Color logoDark = Color(0xFF1E293B);

  // Background
  static const Color background = Color(0xFFF8FAFC);
  static const Color cardBg = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF10B981);  // Present - Green
  static const Color warning = Color(0xFFF59E0B);  // Late - Orange
  static const Color danger = Color(0xFFEF4444);   // Absent - Red
  static const Color info = Color(0xFF3B82F6);     // Info - Blue

  // Text
  static const Color textDark = Color(0xFF1E293B);
  static const Color textLight = Color(0xFF64748B);
  static const Color textWhite = Colors.white;

  // Border
  static const Color border = Color(0xFFE2E8F0);
}
